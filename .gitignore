node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# API keys & local settings
*.key
ai-settings.json

# Uploads
/uploads/
/coach-library/

# Benchmark / experiment exports — können reale Forschungsdaten (Habil-Texte
# etc.) enthalten; werden per Default außerhalb des Repos abgelegt
# (~/sarah-benchmarks/), aber zur Sicherheit hier blockiert, falls jemand das
# Verzeichnis manuell anlegt.
/docs/experiments/benchmark-*/
/docs/experiments/*-data/

# ML model cache
/.model-cache/

# Instance-specific branding (operator logo, impressum HTML, etc.)
# Each deployment provides its own files here; public repo keeps it empty.
/static/brand/*
!/static/brand/.gitkeep
!/static/brand/README.md

# PostgreSQL data
/pgdata/
projekte/
backup-*.sql

# Claude Code local artifacts (Dev-Server-Launch, Browser-Test-Helper)
/.claude/launch.json
/scripts/mint-session.ts
